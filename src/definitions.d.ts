
export interface UserAgentType {
  source: string,
  device: string,
  deviceVersion: string,
  os: string,
  osVersion: string,
  browser: string,
  browserVersion: string,
  isMobile: boolean,
  isTablet: boolean,
  isDesktop: boolean,
  isBot: boolean
}
